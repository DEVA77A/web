# FingerFury Frontend (Type Sprint)

Minimal React + Vite frontend for the typing game demo. Uses a fallback backend when MongoDB is not configured.

Run (development):

```powershell
cd frontend
npm install
npm run dev -- --port 5173 --host
```

Open http://localhost:5173 in your browser.
